using AutoMapper;

namespace Aurora.Services.Mappings
{
    public static class DtoToDomainObjectMappings
    {
        public static void RegisterMaps()
        {
            var config = new MapperConfiguration(cfg =>
            {

            });
        }
    }
}
